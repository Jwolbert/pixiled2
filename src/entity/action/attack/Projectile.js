export default class Projectile extends attack {

    constructor (name, gameObject, entities, physics, attack, interactions) {
        super(name, gameObject, entities, physics, attack, interactions);
    }
};